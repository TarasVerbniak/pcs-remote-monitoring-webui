import React, { Component } from 'react';
import { PageContent } from 'components/shared';

export default class Report extends Component {
  render () {
    const { data } = this.props;

    return <div>Chart is here</div>
  }
}
